import { ListItemContainer } from "./list-item.component.styles";

const ListItem: React.FC = () => {
  return <ListItemContainer>List item component</ListItemContainer>;
};

export default ListItem;
